
{
    "scripts":{
        "pip install": "pip install -r requirements.txt",
        "run migrations": "python src/manage.py migrate",
        "make migrations": "python src/manage.py makemigrations",
        "run django": "python src/manage.py runserver",
        "run black": "black src/ && isort --profile black src/"
    }
}
